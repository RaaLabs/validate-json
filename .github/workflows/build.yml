name: Build Docker Image

on:
  push:
    branches:
      - '**'
  pull_request:
    types: [ closed ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: 
      shell: bash
      run: |
        docker build -f Dockerfile -t testbuild .
